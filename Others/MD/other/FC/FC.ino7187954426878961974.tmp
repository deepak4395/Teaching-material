// (Inline Comment)Arduino Integrated Development Enviornment

/*
   Group Comment
   Lets take number 52
   C, C++
   n=6
   7,8,9,10,11,12
*/
const int n = 6;
int checkNumber = 7 ;
int checkNumber1[n] = {7, 8, 9, 10, 11, 12};
bool result = true ; // false
bool result[n] = {true, true, true, true, true, true} ;
int r;
int Q;
int i = 2;
void setup() {
  // put your setup code here, to run once:
  Serial.begin(9600); //baud rate bits per seconds.
  Q = checkNumber / 2; // +,-, *,
  for (int x = 0; x < n; x = x + 1) {
    Serial.println(x);
  }
  while (i < checkNumber) {
    r = checkNumber % i;
    if (r == 0) {
      // Number is not prime
      result = false;
      break;
    }
    else {
      i = i + 1;
    }
  }
  Serial.print("Result is ");
  Serial.println(result);
  //  Serial.println();
  //  Serial.print("Q= ");
  //  Serial.println(Q);
  //  Serial.print("R= ");
  //  Serial.println(r);
}

void loop() {
  // put your main code here, to run repeatedly:

}
